# ENGRAM Manga Explainer & Brand Identity

## Milestones

| Version | Scope | Status |
|---------|-------|--------|
| **v1** | Soft-color manga explaining the ENGRAM kit itself (Engy mascot, the `update`/`answer` contract, no-hindsight rules) + AI-memory history. | In progress / source style established |
| **v2** | **Two whitepaper mangas** (ALMA & AMA-Bench) on agentic memory + the **ENGRAM // KIT brand identity** and GitHub Pages site, modeled on the `frontier_tower/20260620_ecommerce` reading-pack project. | Shipped (markdown prompts + placeholders + site). See [`ROADMAP.md`](ROADMAP.md). |
| **v3** | **Two linked, branching visual novels** on the site that *teach* memory: a "Learn the Basics" VN (general context-engineering memory — layers + tools) and a "Paper Adventure" VN (interactive ALMA & AMA-Bench), on the vanilla-JS engine validated in spikes 001–003. | **Active — this milestone.** See [`ROADMAP.md`](ROADMAP.md). |

> v1, v2, and v3 share the same cast (**Engy** + **Sensei**) and the same soft-color
> watercolor style, so the books and games read as one series.

## What This Is

A soft-color **manga + brand identity project that makes the ENGRAM memory-system challenge click**, now extended to explaining the **frontier papers ENGRAM is about** and turning that material into **interactive, branching learning games**.

- **v1** turns this repo's abstract concepts (the `update`/`answer` contract, `STATE_DIR`/`MEMORY_DIR`, no-hindsight rules) plus a short history of memory in AI/agentic systems into narrated explainer-panel pages.
- **v2** adds two educational mangas of the agentic-memory whitepapers in [`documents/`](../documents/) — **ALMA** (meta-learning memory designs) and **AMA-Bench** (evaluating long-horizon memory) — written as per-page image-generation prompts under [`documentation/manga/`](../documentation/manga/), plus a stark, typographic **ENGRAM // KIT** brand identity and a GitHub Pages site in [`docs/`](../docs/) that leads with the manga. Panel art is generated by the author in ChatGPT; the markdown prompts and placeholders ship first.
- **v3** turns the static reading material into **two linked, branching visual novels** hosted on the site. A **"Learn the Basics" VN** teaches the universal basics of memory in context engineering — context window vs. short/long-term memory, retrieval (RAG), vector stores, and the **memory layers + the tools at each layer** — with ENGRAM as one worked example. A **"Paper Adventure" VN** turns ALMA & AMA-Bench into an interactive exploration with in-character **Examiner** comprehension checkpoints. Both run on a hand-rolled, zero-dependency vanilla-JS VN engine (validated in spikes 001–003), reuse the ENGRAM // KIT brand, and deploy as GitHub Pages add-ons next to the flipbook. The basics act's new panel art is authored later in ChatGPT (placeholders ship first); the paper adventure reuses the 15 shipped panels via the `extract.js` lettering pipeline.

This is an **educational / content + design project with a light vanilla-JS frontend**, not a backend software build. The existing ENGRAM Kit is the *source material* being explained, not the thing being modified.

## Core Value

After reading the finished manga, a non-expert understands **what ENGRAM is and how a memory system answers questions without hindsight** — the idea finally "clicks" visually. If everything else is cut, the core-idea + two-commands pages must land.

## Requirements

### Validated

(None yet — generate prompts, render in ChatGPT, save graphics to validate that they actually make ENGRAM click)

### Active

- [ ] A recurring **mascot + visual language** is defined once and reused in every page prompt for consistency (mascot = "Engy", a librarian-robot whose notebook chest is MEMORY_DIR; a narrator-sensei explains the mechanics)
- [ ] Page prompts explain **the core idea** — ENGRAM is a memory system that ingests dated workspace states over time and answers without hindsight
- [ ] Page prompts explain **the two-command contract** — `update STATE_DIR MEMORY_DIR` and `answer STATE_DIR MEMORY_DIR QUESTION_JSON ANSWER_JSON`, and what each surface means
- [ ] Page prompts explain **the challenge rules** — no future states, no answer keys, cite evidence paths, distinguish current/stale/superseded/uncertain info
- [ ] Page prompts explain **how to compete** — what a participant builds, the submission folder + `run.sh`, and how `check_submission.py` validates it
- [ ] Page prompts cover a **history of memory in AI/agentic systems, 2020→present** (researched for factual accuracy — RAG, vector DBs, long context, agent memory, etc.)
- [ ] All prompts produce a consistent **soft-color manga** look (light watercolor/anime palette) suitable for saving page by page
- [ ] Each page's prompt is **stored as a GSD spike** under `.planning/spikes/`, with room to drop the saved graphic alongside it
- [ ] Full first pass is **15+ pages**, sequenced as a coherent explainer

### Active — v2 (this milestone)

- [ ] **ALMA whitepaper manga** — an 8-page soft-color explainer of *Learning to Continually Learn via Meta-learning Agentic Memory Designs* (arXiv 2602.07755), as per-page image-generation prompts under `documentation/manga/alma/`
- [ ] **AMA-Bench whitepaper manga** — a 7-page soft-color explainer of *Evaluating Long-Horizon Memory for Agentic Applications* (arXiv 2602.22769), under `documentation/manga/ama-bench/`
- [ ] Both whitepaper mangas reuse the shared **Engy + Sensei** cast and the locked soft-color/watercolor style block for series consistency
- [ ] **ENGRAM // KIT brand identity** — written spec (`docs/BRAND.md`) + self-demonstrating styleguide (`docs/brand-guidelines.html`): monochrome + rationed recall-green (#00e0a4), Helvetica, the `//` mark, terminal/archive aesthetic
- [ ] **GitHub Pages site** (`docs/`) modeled on the ecommerce reading-pack: manga-first landing (`index.html`), multi-book flipbook (`flipbook.html`), shared CSS/JS, `.nojekyll`
- [ ] **Manga skills vendored** into `.claude/skills/` (educational-manga-generator, manga-pdf-generator, socratic-reading) so the repo is self-contained
- [ ] **Placeholder panels + PDFs** ship now (labeled stand-ins at the exact filenames the site/flipbook/PDF expect); author drops real ChatGPT panels over them later

### Active — v3 (this milestone)

- [ ] **Reusable VN engine** — promote the spike-001/002/003 runner into a shared, brand-native `docs/` module (typewriter, click/space advance, branching, Examiner `{quiz}` checkpoints, forensic/learning telemetry export), zero dependencies, `file://`-runnable
- [ ] **"Learn the Basics" VN** — a branching teaching game covering the universal basics of memory in context engineering: context window vs. short/long-term memory, retrieval (RAG), vector stores, and the **memory layers + the tools at each layer**, with ENGRAM as one worked example
- [ ] **Basics VN content** — new per-page image-generation prompts (`documentation/manga/basics/page-NN.md`) reusing the Engy + Sensei cast and locked soft-color style; labeled placeholder panels ship now, author renders real panels in ChatGPT later
- [ ] **"Paper Adventure" VN** — an interactive ALMA & AMA-Bench experience built from the existing 15 manga pages via the validated `extract.js` lettering→script pipeline, with diegetic Examiner comprehension checkpoints layered on top
- [ ] **Branching + comprehension design** — both VNs use real branching choices and per-distractor corrective feedback (not passive replay); first-try-correct vs. total-attempts is the mastery signal
- [ ] **Site integration** — both VNs are linked from the landing page as GitHub Pages add-ons next to the flipbook, ENGRAM // KIT brand-consistent, with no build step
- [ ] **Authoring convention enforced** — "one utterance per `- Panel N — Speaker:` bullet" so lettering→VN extraction stays lossless (touch up the one known dual-utterance line)

### Out of Scope

- **Modifying the ENGRAM Kit code** (`check_submission.py`, the contract, sample states) — this project explains the kit, it does not change it
- **Auto-generating the images programmatically** (API image generation) — the author generates in ChatGPT and saves graphics manually by choice
- **A character-driven plot/story** — chosen format is narrated explainer panels, not a narrative arc
- **Print layout / typesetting / lettering tooling** — out of scope for the first pass; prompts target single page images
- **Building an actual ENGRAM submission** (a real memory system) — separate effort; this is the explainer only
- **v3:** A heavyweight VN engine (Monogatari / Twine / Ren'Py) — rejected in spike 001; the hand-rolled vanilla-JS runner is the locked approach for a zero-build `docs/` add-on
- **v3:** Accounts, backend, server-side save/load or progress sync — telemetry is client-side export only; the VNs open via `file://` and on GitHub Pages with no backend
- **v3:** Auto-generating the basics-act panels via image API — same human-in-the-loop constraint as the manga; the author renders in ChatGPT by choice
- **v3:** A single merged mega-VN — chosen structure is **two linked VNs** (basics, adventure), each its own page linked from the landing

## Context

- **Source material lives in this repo** and is already mapped in `.planning/codebase/`. Key inputs for accurate manga content:
  - `CONTRACT.md` — the two-command interface (authoritative)
  - `STATE_FORMAT.md`, `QUESTION_FORMAT.md`, `SUBMISSION_FORMAT.md` — the data surfaces and JSON shapes
  - `README.md` — the framing of ENGRAM as a time-sequence memory challenge
  - `examples/simple_memory/` and `check_submission.py` — what a submission and validation look like
- **Author's goal is comprehension.** The author finds the kit and its context hard to understand; the manga is a tool to make it click. Clarity beats cleverness on every page.
- **Workflow is human-in-the-loop:** GSD produces the prompt (spike) → author pastes into ChatGPT → author saves the rendered page manually. The project never calls an image API.
- **History section needs research** to be accurate across 2020→present (RAG, vector stores, long-context models, agent memory frameworks like MemGPT/Letta, etc.).

## Constraints

- **Tooling**: Image generation is **ChatGPT, manual** — prompts must be self-contained, copy-paste ready, and reproducible page to page (re-state the mascot/style block in each prompt so a fresh ChatGPT session stays consistent).
- **Consistency**: Soft-color manga style + the "Engy" mascot must be **pinned once and repeated** in every prompt; drift between pages is the main quality risk.
- **Accuracy**: ENGRAM mechanics must match the kit exactly (commands, file surfaces, rules). The history timeline must be factually grounded, not hallucinated.
- **Storage**: Prompts are **GSD spikes** under `.planning/spikes/`, one per page, leaving space to save the graphic next to its prompt.
- **Audience**: Non-expert / first-time reader — minimize jargon; define terms visually.

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Output is ChatGPT image prompts, not rendered images | Author renders + saves manually; GSD's job is high-quality, consistent prompts | — Pending |
| One prompt per page (multi-panel page) | Author's chosen packaging; easier to save page by page | — Pending |
| Format = narrated explainer panels (not story) | Clearer for technical flow; author chose this over character-driven plot | — Pending |
| Soft-color manga style, pinned in every prompt | Friendly + approachable; re-stating style block prevents page-to-page drift | — Pending |
| Recurring mascot "Engy" (notebook = MEMORY_DIR) + narrator-sensei | Makes abstract memory concepts physical and visual; author asked me to design it | — Pending |
| Store each page prompt as a GSD spike | Fits the GSD workflow already in use; keeps prompt + saved graphic together | — Pending |
| Research the 2020→present AI-memory history before writing those pages | Timeline must be factually accurate, not invented | — Pending |
| **v2:** Explain the two agentic-memory whitepapers as their own mangas | The papers in `documents/` are what ENGRAM is *about*; mangas make them click and bracket ENGRAM from the design (ALMA) and evaluation (AMA-Bench) sides | ✓ Markdown shipped |
| **v2:** Reuse Engy + Sensei across all books (one series) | Cross-book visual consistency; the mascot already defined for v1 carries over | ✓ Applied |
| **v2:** Brand identity = **ENGRAM // KIT**, lab/terminal, recall-green #00e0a4 | New identity distinct from the ecommerce `AGENTIC//COMMERCE`, same Cargo-grade caliber; green = memory/recall/answer | ✓ Shipped |
| **v2:** Manga style = soft-color Engy mascot (not ecommerce gekiga) | User-chosen; keeps v2 consistent with the existing ENGRAM explainer milestone | ✓ Applied |
| **v2:** Model the site + skills on `frontier_tower/20260620_ecommerce` | Proven structure (docs/ Pages site, flipbook, vendored manga skills); copy the approach, not the brand | ✓ Done |
| **v2:** Ship markdown + placeholders first, panels later | Author generates panels in ChatGPT by hand; markdown prompts and labeled placeholders unblock the site/flipbook/PDF today | ✓ Done |
| **v3:** Interactive learning VNs over a passive flipbook | Retrieval practice + corrective feedback (spike 002) reinforce concepts better than re-reading; a learning VN must earn its place over the flipbook with interactivity | — Pending |
| **v3:** Hand-rolled vanilla-JS VN engine (no Monogatari/Twine/Ren'Py) | Zero-build `docs/` add-on, `file://`-runnable, full brand control; spike 001 build-vs-buy survey rejected the engines as overweight | — Pending |
| **v3:** Two linked VNs (basics + adventure), not one merged game | User-chosen; each is a focused entry point on the landing page — basics teaches universals, adventure explores the papers | — Pending |
| **v3:** Basics teaches *general* context-engineering memory, ENGRAM as one example | User-chosen; the layers/tools framing generalizes beyond the kit, then lands on ENGRAM as the worked example | — Pending |
| **v3:** Reuse the manga lettering→script pipeline for the adventure | Spike 003 proved 87 lines / 15 pages auto-extract ~99% clean; the adventure dialogue is a byproduct of the shipped manga, not authored from scratch | — Pending |
| **v3:** New basics panels authored later; placeholders ship first | Same human-in-the-loop art constraint as v2; markdown prompts + placeholders unblock the VN engine and site wiring today | — Pending |

## Evolution

This document evolves at phase transitions and milestone boundaries.

**After each phase transition** (via `/gsd-transition`):
1. Requirements invalidated? → Move to Out of Scope with reason
2. Requirements validated? → Move to Validated with phase reference
3. New requirements emerged? → Add to Active
4. Decisions to log? → Add to Key Decisions
5. "What This Is" still accurate? → Update if drifted

**After each milestone** (via `/gsd-complete-milestone`):
1. Full review of all sections
2. Core Value check — still the right priority?
3. Audit Out of Scope — reasons still valid?
4. Update Context with current state

---
*Last updated: 2026-06-22 — v3 milestone started (two linked teaching visual novels: "Learn the Basics" + "Paper Adventure"), grounded in spikes 001–003. See [`ROADMAP.md`](ROADMAP.md).*

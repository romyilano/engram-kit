Page 06

Purpose:
The fix — AMA-Agent builds a causality graph (actions → state transitions → observations) and uses tool-augmented retrieval, reaching 57.22% accuracy and beating the strongest baseline by 11.16%.

Audience note:
Builds on page 5 (why similarity-only fails). "Causality graph" = a map of what caused what; "tool-augmented retrieval" = using tools (e.g. queries) to fetch the right facts, not just similarity.

Panel Count:
6

Image Prompt:

Create a six-panel educational manga page.

Style:
Soft-color educational manga.
Light watercolor / gentle anime palette — warm cream paper, soft ink linework, muted pastel washes.
One restrained accent: recall-green (#00e0a4), used only for memory, recall, and "it worked" moments.
Clean confident linework, generous negative space, friendly but precise.
Calm, contemplative, explanatory tone — diagrams integrated naturally into the scene.
Not gritty. Not neon. Not chibi. Not glossy 3D. Not photorealistic.

Characters in this page:
- ENGY: a small, rounded librarian-robot with a soft matte-cream chassis, one big recall-green camera-lens eye, stubby arms, an open NOTEBOOK in its chest (its memory) — here the notebook is upgraded into a linked graph.
- THE EXAMINER: a poised proctor figure holding sealed question-cards; a scoreboard slate showing percentages.
- SENSEI: a calm middle-aged human archivist with round glasses, a soft cardigan, neat greying hair, holding a slim wooden pointer.

Setting:
The examination hall. Engy's notebook visibly restructures into a graph; a comparison bar appears on the scoreboard.

Panel 1 (medium):
Sensei introduces the upgrade; Engy's chest-notebook pages reconnect into a NODE-AND-EDGE GRAPH — actions, state transitions, and observations as linked nodes, recall-green causal edges between them. Dialogue space.

Panel 2 (close, causality graph):
Detail of the graph: an "action" node connects to the "state change" it caused, which connects to the "observation" that followed. The causal chain is now explicit and walkable.

Panel 3 (medium, tool-augmented retrieval):
Instead of grabbing a look-alike note, Engy uses a little TOOL (a query lens / search tool) to fetch the exact causal node it needs — following edges, not just similarity. Recall-green hit on the right node.

Panel 4 (the re-test):
The same question from page 5 ("why is the gate open?"); this time Engy walks the causal edge back to the unlock action and answers correctly — eye glowing steady recall-green. Examiner marks it right.

Panel 5 (scoreboard comparison):
Sensei's pointer at the scoreboard: AMA-Agent reaches 57.22%, a bar clearly taller than the strongest baseline, with "+11.16%" called out in recall-green.

Panel 6 (two-shot, the takeaway):
Engy proud, Examiner approving. The method that respects causality and uses tools wins. Dialogue space.

Camera directions:
Make the notebook→graph transformation the visual centerpiece (panels 1–2); show the tool fetching the *right* node by following edges; reserve recall-green for correct recalls and the +11.16% callout.

Lettering:
Leave room for English dialogue and captions.
- Panel 1 — Sensei: "What if memory kept the *causes*, not just look-alike notes?"
- Panel 2 — (caption) "AMA-Agent builds a *causality graph*: action → state change → observation."
- Panel 3 — Engy: "And I can use a *tool* to fetch the exact node — by following the cause, not the resemblance."
- Panel 4 — Engy: "The gate is open *because* I unlocked it three steps back." — Examiner: "Correct."
- Panel 5 — Sensei: "57.22% — eleven points over the best baseline."
- Panel 6 — Engy: "Keep the causes. Retrieve with tools. *That's* the difference."

Output:
A single finished manga page.
